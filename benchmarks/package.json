{
  "name": "aeon-benchmarks",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "bench": "vitest bench --run",
    "bench:watch": "vitest bench",
    "report": "tsx src/report.ts",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@most/core": "^1.6.1",
    "@most/scheduler": "^1.3.0",
    "@most/types": "^1.1.0",
    "aeon-core": "workspace:*",
    "aeon-scheduler": "workspace:*",
    "aeon-types": "workspace:*",
    "rxjs": "^7.8.2"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
